name: Refresh catalogue and deploy

on:
  schedule:
    # Hourly. Fast enough to track an activation without generating dozens of
    # commits a day. Run the workflow manually if something has just been
    # released and you do not want to wait.
    - cron: '7 * * * *'
  push:
    branches: [main]
  workflow_dispatch:
  # GitHub's scheduler has proved unreliable for this repo (zero schedule events
  # in the first 12 hours despite a valid active cron), so allow an external
  # trigger that does not depend on it:
  #   gh api repos/OWNER/REPO/dispatches -f event_type=refresh
  repository_dispatch:
    types: [refresh]

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: refresh
  cancel-in-progress: false

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Rebuild event catalogues
        id: build
        # Exit 2 means an upstream API was unavailable and the previous
        # catalogue was deliberately kept. That is not a failure worth
        # alerting on, but it should not be silent either.
        run: |
          set +e
          python3 scripts/build_catalog.py
          code=$?
          set -e
          if [ "$code" -eq 2 ]; then
            echo "::warning::One or more catalogues kept stale (upstream unavailable)"
          elif [ "$code" -ne 0 ]; then
            exit "$code"
          fi

      - name: Commit if changed
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          if [ -n "$(git status --porcelain data/ index.html ./*/index.html)" ]; then
            git add data/ index.html ./*/index.html
            git commit -m "chore: refresh catalogue $(date -u +%Y-%m-%dT%H:%MZ)"
            git push
          else
            echo "No catalogue change."
          fi

      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: .
      - id: deployment
        uses: actions/deploy-pages@v4
